<div>
	<h4>
		{{ product.name }}
		<img src="/assets/images/{{ product.image }}" alt="{{ product.name }}">

		<a href="{{ path('cart_add', {'id':product.id}) }}">
			Ajouter au panier
		</a>
	</h4>
</div>
