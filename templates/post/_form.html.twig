<div class="form-post">
	{{ form_start(form) }}
	<div class="row">
		<div class="col">
			{{ form_row(form.title) }}
		</div>
		<div class="col">
			{{ form_row(form.image) }}
		</div>
	</div>
	<div class="row mt-4">
		{{ form_row(form.content) }}
	</div>
	<div class="row mt-4">
		{{ form_row(form.link) }}
	</div>
	<div class="row mt-4">
		{{ form_row(form.isPinned) }}
	</div>
	<div class="row mt-4">
		{{ form_row(form.submit) }}
	</div>
	<br>
	{{ form_end(form) }}
</div>
