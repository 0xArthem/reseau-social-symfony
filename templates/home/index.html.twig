{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!
{% endblock %}

{% block body %}
	<div>
		<h1>Mon réseau social</h1>

		{% if app.user %}
			<a href="{{ path('app_account', {'username': app.user.username}) }}" type="button">
				Voir mon profil
			</a>
		{% else %}
			<a href="{{ path('app_login') }}">Se connecter</a>
		{% endif %}

		<hr>

		{% if app.user %}
			<section>
				<h3>Les derniers posts de mes abonnements</h3>
			</section>

			<section class="posts-TL">
				{% for abonnementsPost in abonnementsPosts %}
					{{ abonnementsPost.title }}
				{% endfor %}
			</section>
		{% endif %}
	</div>
{% endblock %}
